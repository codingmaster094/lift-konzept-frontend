/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['lift-konzept-backend.vercel.app'],
    formats: ['image/webp'],
    unoptimized: true,
  },
};

export default nextConfig;
